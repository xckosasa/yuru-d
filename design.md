# ゆるっと体重ログ UI / 実装方針

## 現在の位置づけ

「LOGS」は、毎日の体重・体脂肪率を記録し、BMIや体重推移を軽く確認できるスマホ向けPWAです。

初期案から実装が進んでおり、現在の画面構成・機能を正とします。今後の制作では、既存のUIと機能を活かしながら、スマホでの入力しやすさ、見やすさ、継続しやすさを優先して調整してください。

## アプリの方向性

このアプリは、ストイックなダイエット管理アプリではなく「毎日ひとつだけ記録を残す」ための軽い健康ログです。

目指す印象:

* 体重計に乗ったあと、すぐ入力できる
* 数字の変化が見やすい
* 評価や警告を押しつけない
* ホーム画面に置いて毎日開きやすい
* シンプルだが、記録を続けるための最低限の便利さがある

避けたい印象:

* ジムアプリのようにストイック
* 医療アプリのように硬い
* ダッシュボード感が強すぎる
* 体重増減を強く評価する
* 派手な装飾や過度なアニメーション

## 現在の主な機能

実装済みの機能は以下です。

* 体重・体脂肪率・メモ・日付の記録
* 同じ日付の記録の上書き保存
* localStorageへの記録保存
* 最新記録のサマリー表示
* BMI表示
* 前回比の表示
* 目標体重との差分表示
* SVGによる体重推移グラフ
* 記録一覧表示
* 記録の編集
* 記録の削除
* 全記録クリア
* 身長・目標体重・通知予定時刻の設定
* プライベートモード
* ダークモード
* 保存時の軽いフィードバック
* PWA関連ファイル、Service Worker、アイコン

## レイアウト方針

スマホファーストで設計します。

主対象:

* 360pxから430px程度のスマホ幅
* PC表示時は中央寄せのスマホ幅レイアウト

現在の基本構成:

1. ヘッダー
2. 今日のサマリーカード
3. 今日の記録フォーム
4. 体重推移グラフ
5. 記録一覧
6. 設定モーダル
7. 編集モーダル

全体方針:

* 1ページ構成を維持する
* 主要コンテンツはカード単位で整理する
* スマホで縦に読みやすい順序を優先する
* ボタンや入力欄はタップしやすい高さにする
* PCではスマホ画面をそのまま中央に置く感覚を維持する
* 設定や編集など、日常入力の邪魔になる操作はモーダルに逃がす

## ビジュアル方針

現在はミントグリーンを強く使った、明るくポップ寄りの見た目です。この方向をベースにしつつ、健康管理アプリとして圧が出すぎないように調整します。

色の考え方:

* メインカラーはミントグリーン系
* 背景にもメインカラーを使う場合は、カードとのコントラストを確保する
* アクセントカラーは強く使いすぎず、保存・強調・グラフの補助に限定する
* エラー色は強い赤ではなく、やわらかい赤みで表示する
* テキストは真っ黒に寄せすぎず、読みやすい濃いグレーを基本にする
* ダークモード時もカード、入力欄、グラフ線の視認性を保つ

カード:

* 角丸は大きめ
* 影は弱め
* 余白は広め
* カード内の情報量を詰め込みすぎない
* 記録一覧の1件ごとのカードは、日付・体重・体脂肪率・メモ・前回比が読み取れることを優先する

タイポグラフィ:

* 日本語UIとして自然なフォント指定を使う
* system-ui、-apple-system、Noto Sans JP、Segoe UI系を基本にする
* 見出しは少し太め
* 数値は大きく、単位は控えめに見せる
* 小さな補足文は読みづらくならないサイズを保つ

## コンポーネント方針

### ヘッダー

現在はロゴ画像と設定ボタンを配置しています。

* ロゴをアプリの顔として扱う
* サブコピーは短く、朝の記録を促す程度にする
* 設定ボタンはアイコンボタンとして右上に置く
* ヘッダー自体は大きくしすぎず、すぐ記録フォームに到達できるようにする

### 今日のサマリーカード

最新記録がある場合に、以下を表示します。

* 最新体重
* BMI
* 前回比
* 目標体重との差分
* 体脂肪率

プライベートモード時:

* 体重の数値は表示しない
* 初日からの差分など、直接体重を出さない情報を表示する
* 「体重は非公開です」のように状態が分かる補足を出す

表現方針:

* 増減を評価しない
* 「太った」「痩せた」のような言い方は避ける
* 「前回から +0.3kg」「目標まで あと 2.5kg」のように、事実だけを淡々と表示する

### 今日の記録フォーム

入力項目:

* 日付
* 体重 kg
* 体脂肪率 %
* メモ

方針:

* 入力欄は大きく、スマホで押しやすくする
* labelとinputを分かりやすく配置する
* kgや%などの単位が分かる文言にする
* 体脂肪率とメモは任意
* 保存ボタンは目立たせるが、強い警告色にはしない
* 保存後は「記録しました」のような短いフィードバックを出す

入力エラー:

* 日付未入力
* 体重未入力または不正な値
* 体脂肪率が不正な値

エラー文言は強く責めず、入力し直せばよいことが分かる表現にします。

### 体重推移グラフ

現在はSVGで折れ線グラフを表示しています。

表示条件:

* 記録が2件以上ある場合にグラフを表示
* 記録が少ない場合はプレースホルダー文言を表示

グラフ方針:

* 横軸は日付
* 点には体重ラベルを表示
* 目標体重が設定されている場合は目標ラインを表示
* グリッド線は控えめにする
* グラフライブラリは現時点では不要。既存のSVG実装を維持する

### 記録一覧

表示方針:

* 新しい日付を上に表示する
* 1件ごとにカード形式で表示する
* 体重、体脂肪率、メモ、前回比を表示する
* プライベートモード時は体重を非公開表示にする

操作:

* 各記録から編集モーダルを開ける
* 編集モーダル内で削除できる
* 削除時はconfirmで確認する

### 設定モーダル

設定は画面内の常設セクションではなく、右上の設定ボタンから開くモーダルとして扱います。

設定項目:

* 体重を非公開にする
* ダークモード
* 身長 cm
* 目標体重 kg
* 通知したい時間

操作:

* 設定を保存
* 設定をリセット
* ログをクリア

方針:

* 設定値はlocalStorageに保存する
* 通知時刻は「毎日この時間に記録する予定」として表示する
* 通知機能はブラウザのNotification APIとService Workerを使う
* 通知が許可されていない場合でも、記録機能の利用を妨げない

### 編集モーダル

記録一覧の編集ボタンから開きます。

編集できる項目:

* 体重
* 体脂肪率
* メモ

日付は既存記録のキーとして扱うため、編集モーダルでは変更不可にします。

編集モーダル内でできる操作:

* 保存
* 削除
* 閉じる

## 文言ルール

基本方針:

* やさしく自然な日本語にする
* ユーザーの状態を評価しない
* 健康リスクの断定をしない
* 数値の変化は事実として表示する
* 操作文言は短く分かりやすくする

使ってよい文言例:

* 今日の記録
* 記録しました
* まだ記録がありません
* 前回から -0.3kg
* 目標まで あと 2.5kg
* 体重は非公開です
* 毎日 07:00 に記録する予定

避ける文言例:

* 肥満です
* 危険です
* 失敗です
* 太りました
* もっと頑張りましょう
* サボっています

## データ保存方針

現時点ではサーバー連携を行わず、localStorageで保存します。

保存対象:

* 記録一覧
* 身長
* 目標体重
* 通知予定時刻
* プライベートモード
* ダークモード

記録データ:

* date
* weight
* fat
* note

同じ日付の記録は上書き保存します。

## アクセシビリティ方針

* 入力欄にはラベルを付ける
* ボタンはタップしやすいサイズを保つ
* アイコンボタンは意味が分かるようにする
* モーダルは背景クリックと閉じるボタンで閉じられる
* エラー文言は色だけに頼らず、テキストで伝える
* グラフにはaria-labelを付ける

## 実装方針

* Vue 3のComposition APIを使う
* 1ページ構成を維持する
* CSSは`src/assets/style.scss`に集約する
* 既存のユーティリティ関数を活かす
* 体重計算やlocalStorage操作はコンポーネント内に増やしすぎない
* グラフは`WeightChart.vue`に閉じ込める
* 破壊的な操作には確認を入れる
* 過度なアニメーションは入れない
* 保存時や新規記録時の軽いフィードバックは維持する

## 今後の調整候補

優先度が高いもの:

* 現在の強めの色使いを、見やすさを保ったまま少し落ち着かせる
* 文言を全体的に「ゆるっと」したトーンにそろえる
* 設定モーダル内の危険操作を視覚的に分ける
* プライベートモード時の表示をさらに自然にする
* ダークモード時の入力欄、カード、グラフのコントラストを確認する

必要になったら検討するもの:

* 記録のエクスポート
* 通知許可の状態表示
* 月別・週別の表示切り替え
* メモ検索
* 目標体重の非表示設定
* バックアップや同期

## 完成イメージ

スマホで開いたときに、体重記録のための小さな習慣アプリとして自然に使える状態を目指します。

* 朝、体重計に乗ったあとにすぐ入力できる
* 最新の状態がひと目で分かる
* グラフで流れを軽く確認できる
* 記録の修正もすぐできる
* 数字に圧を感じない
* 毎日少しだけ続けられそう
